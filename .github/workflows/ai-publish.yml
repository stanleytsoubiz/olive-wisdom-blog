name: "Auto Deploy Blog (DISABLED - replaced by nextjs-deploy.yml)"

# ⛔ 此 workflow 已停用
# 原用途：部署舊靜態 HTML 版本（deploy/ 目錄）至 Cloudflare Pages
# 現況：系統已升級為 Next.js + opennextjs-cloudflare，由 nextjs-deploy.yml 統一負責
# 停用原因：此 workflow 會覆蓋 Next.js 的部署輸出，造成衝突

on:
  workflow_dispatch:
    inputs:
      message:
        description: '(已停用) 此 workflow 不再使用'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: "Notice"
        run: echo "此 workflow 已停用。請使用 nextjs-deploy.yml。"
